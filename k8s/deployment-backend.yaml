#k8s/deployment-backend.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: topscorers-backend
  namespace: topscorers
spec:
  replicas: 1
  selector:
    matchLabels: { app: topscorers-backend }
  template:
    metadata:
      labels: { app: topscorers-backend }
    spec:
      containers:
        - name: api
          image: ghcr.io/drokenhorisk/topscorers-backend:0e9a3e9428a50a8371c09c519575882750de0b9d
          imagePullPolicy: Always
          envFrom:
            - configMapRef: { name: topscorers-config }
            - secretRef: { name: topscorers-secrets }
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: topscorers-data
